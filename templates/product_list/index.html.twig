{% extends 'base.html.twig' %}

{% block title %}Sezname Productu !{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Seznam Productu ! ✅</h1>

    <table class="table">
        <tbody>
        {%  for produkt in product %}
        <tr>
            {#<th>Name</th>#}
            <td> Name: </td>
            <td>
                <a href="{{ path('product_show', {'id': produkt.id}) }}">
                    {{ produkt.name }}
                </a>
            </td>
        </tr>
        </tbody>
    </table>
    {%  endfor %}
</div>
{% endblock %}
